services:
  db:
    image: postgres:18
    container_name: postgres-dev
    env_file: .env
    ports :
      - "5432:5432"
    volumes:
      - ./init-db:/docker-entrypoint-initdb.d
      - postgres_data:/var/lib/postgresql
volumes:
  postgres_data:
      
