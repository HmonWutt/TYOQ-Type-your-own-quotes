## Save current stats and quote

### Requisites

1. a config file 
2. A way to identify a quote
  - Candidates
    - quote id *easy as long as quote database stays the same but update to quote database will change id*
    - hashing on quote text *will work with database update; this will also help with duplicate quotes*

## Random quote selection
